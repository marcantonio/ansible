#!/bin/bash

set -euo pipefail

# LOCK_FILE="${XDG_STATE_HOME:-$HOME/.local/state}/first-login.lock"
# if [[ -e "$LOCK_FILE" ]]; then
#   exit 0
# fi

# mkdir -p "$(dirname "$LOCK_FILE")"

# umask 077
# : > "$LOCK_FILE"

### User actions

# Networking
